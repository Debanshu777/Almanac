<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.CalenderView" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/base_yellow</item>
        <item name="colorPrimaryVariant">@color/base_green</item>
        <item name="colorOnPrimary">@color/darker_grey</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/base_orange</item>
        <item name="colorSecondaryVariant">@color/base_green</item>
        <item name="colorOnSecondary">@color/white</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">@color/base_yellow</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Theme.CalenderView.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <style name="splashScreenTheme" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_image</item>
        <item name="android:statusBarColor">@color/base_yellow</item>
    </style>
    <style name="Theme.CalenderView.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="Theme.CalenderView.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />
</resources>